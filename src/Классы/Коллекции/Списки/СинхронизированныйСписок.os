#Использовать "../../../internal"

Перем ВнутреннийСписок;  // Оригинальная коллекция
Перем Блокировка;           // Блокировка для синхронизации доступа

// Возвращает итератор обёрнутого списка
//
// Итератор не захватывает блокировку, поэтому обход требует внешней синхронизации,
//  изменение списка из другого потока во время обхода приводит к исключению
//
//  Возвращаемое значение:
//   СписокИтератор - Итератор списка
//
Функция Итератор() Экспорт
	Возврат ВнутреннийСписок.Итератор();
КонецФункции

// Выполняет переданный алгоритм для каждого элемента списка
//
// Блокировка удерживается на протяжении всего обхода, поэтому длительный алгоритм
//  блокирует остальные потоки
//
// Параметры:
//   Алгоритм - Действие - Делегат на процедуру с одним параметром, в который
//    будет передан элемент списка
//            - Строка - Лямбда выражение с одним параметром, в который
//    будет передан элемент списка
//   Контекст - Структура - Контекст добавляемый в лямбда выражение, значения будут доступны по обращению
//    к ключу как к переменной.
//    При передачи действия в первом параметре, этот параметр игнорируется
//            - Сценарий - Экземпляр объекта, который будет захвачен в лямбда выражение, в лямбда выражении
//    будет доступен контекст (публичные и приватные поля, публичные методы) объекта.
//    При передачи действия в первом параметре, этот параметр игнорируется
//
// Пример:
//    список.ДляКаждого("Элемент -> Сообщить(Элемент)");
//
Процедура ДляКаждого(Алгоритм, Контекст = Неопределено) Экспорт
	
	Блокировка.Заблокировать();

	Попытка
		ВнутреннийСписок.ДляКаждого(Алгоритм, Контекст);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Разблокировать();

КонецПроцедуры

// Проверяет содержится ли указанный элемент в списке
//
// Параметры:
//   Элемент - Произвольный - Проверяемый элемент
//
//  Возвращаемое значение:
//   Булево - Истина, если список содержит указанный элемент
//
Функция Содержит(Элемент) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Содержит(Элемент);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Проверяет что список содержит все элементы переданной коллекции
//
// Проверка выполняется целиком под блокировкой
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой проверяются на вхождение
//
//  Возвращаемое значение:
//   Булево - Истина, если список содержит все элементы переданной коллекции
//
Функция СодержитВсе(Коллекция) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.СодержитВсе(Коллекция);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Проверяет что список пустой
//
//  Возвращаемое значение:
//   Булево - Истина, если список пуст
//
Функция Пусто() Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Пусто();
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;
	
КонецФункции

// Возвращает процессор коллекции по элементам обёрнутой списка
//
// В отличие от остальных методов, вызов выполняется без блокировки
//
//  Возвращаемое значение:
//   ПроцессорКоллекций - Процессор коллекции
//
Функция ПроцессорКоллекции() Экспорт
	Возврат ВнутреннийСписок.ПроцессорКоллекции();
КонецФункции

// Возвращает количество элементов списка
//
//  Возвращаемое значение:
//   Число - Количество элементов списка
//
Функция Количество() Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Количество();
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Выгружает элементы списка в массив
//
// Выгрузка выполняется под блокировкой, поэтому полученный массив является
//  согласованным снимком списка
//
//  Возвращаемое значение:
//   Массив из Произвольный - Массив элементов списка
//
Функция ВМассив() Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.ВМассив();
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Добавляет элемент в конец списка
//
// Параметры:
//   Элемент - Произвольный - Добавляемый элемент
//
//  Возвращаемое значение:
//   Булево - Истина, если элемент добавлен
//
Функция Добавить(Элемент) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Добавить(Элемент);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Добавляет все элементы из переданной коллекции
//
// Элементы добавляются под общей блокировкой, то есть атомарно относительно других потоков
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой требуется добавить
//
//  Возвращаемое значение:
//   Булево - Истина, если был добавлен хотя бы один элемент
//
Функция ДобавитьВсе(Коллекция) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.ДобавитьВсе(Коллекция);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Удаляет все элементы списка
//
Процедура Очистить() Экспорт

	Блокировка.Заблокировать();

	Попытка
		ВнутреннийСписок.Очистить();
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Разблокировать();

КонецПроцедуры

// Удаляет переданный элемент из списка
//
// Параметры:
//   Элемент - Произвольный - Удаляемый элемент
//
//  Возвращаемое значение:
//   Булево - Истина, если элемент был удалён
//
Функция Удалить(Элемент) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Удалить(Элемент);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Удаляет все элементы которые содержатся в переданной коллекции
//
// Элементы удаляются под общей блокировкой, то есть атомарно относительно других потоков
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой будут удалены
//
//  Возвращаемое значение:
//   Булево - Истина, если был удалён хотя бы один элемент
//
Функция УдалитьВсе(Коллекция) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.УдалитьВсе(Коллекция);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Удаляет элементы для которых функция предикат вернёт Истина
//
// Блокировка удерживается на протяжении всего удаления, поэтому длительный предикат
//  блокирует остальные потоки
//
// Параметры:
//   Предикат - Действие - Делегат на функцию с одним параметром, в которую передаётся элемент списка
//            - Строка - Лямбда выражение с одним параметром, в который передаётся элемент списка
//   Контекст - Структура - Контекст добавляемый в лямбда выражение, значения будут доступны по обращению
//    к ключу как к переменной.
//    При передачи действия в первом параметре, этот параметр игнорируется
//            - Сценарий - Экземпляр объекта, который будет захвачен в лямбда выражение, в лямбда выражении
//    будет доступен контекст (публичные и приватные поля, публичные методы) объекта.
//    При передачи действия в первом параметре, этот параметр игнорируется
//
//  Возвращаемое значение:
//   Булево - Истина, если был удалён хотя бы один элемент
//
// Пример:
//    список.УдалитьЕсли("(Элемент) -> Элемент >= 2");
//
//    список.УдалитьЕсли("(Элемент) -> Элемент >= Предел", Новый Структура("Предел", 2));
//
Функция УдалитьЕсли(Предикат, Контекст = Неопределено) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.УдалитьЕсли(Предикат, Контекст);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Удаляет все элементы которые не содержатся в переданной коллекции
//
// Элементы удаляются под общей блокировкой, то есть атомарно относительно других потоков
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой будут сохранены
//
//  Возвращаемое значение:
//   Булево - Истина, если был удалён хотя бы один элемент
//
Функция СохранитьВсе(Коллекция) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.СохранитьВсе(Коллекция);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Получает элемент списка по индексу
//
// Вызывает исключение, если индекс выходит за границы списка
//
// Параметры:
//   Индекс - Число - Индекс, по которому требуется получить элемент списка
//
//  Возвращаемое значение:
//   Произвольный - Элемент списка по переданному индексу
//
Функция Получить(Индекс) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Получить(Индекс);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Возвращает индекс первого вхождения переданного элемента
//
// Параметры:
//   Элемент - Произвольный - Элемент, индекс которого требуется получить
//
//  Возвращаемое значение:
//   Число - Индекс элемента списка, или -1 если список не содержит элемент
//
Функция Индекс(Элемент) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Индекс(Элемент);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Возвращает индекс последнего вхождения переданного элемента
//
// Параметры:
//   Элемент - Произвольный - Элемент, индекс которого требуется получить
//
//  Возвращаемое значение:
//   Число - Индекс элемента списка, или -1 если список не содержит элемент
//
Функция ПоследнийИндекс(Элемент) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.ПоследнийИндекс(Элемент);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Возвращает максимальный индекс списка
//
// Для пустого списка возвращает -1
//
//  Возвращаемое значение:
//   Число - Максимальный индекс списка
//
Функция ВГраница() Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.ВГраница();
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Вставляет элемент в список по индексу
//
// Вызывает исключение, если индекс выходит за границы списка
//
// Параметры:
//   Индекс  - Число - Индекс, по которому будет вставлен элемент
//   Элемент - Произвольный - Вставляемый элемент
//
//  Возвращаемое значение:
//   Булево - Истина, если элемент вставлен
//
Функция Вставить(Индекс, Элемент) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Вставить(Индекс, Элемент);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Вставляет все элементы из коллекции начиная с индекса
//
// Элементы вставляются под общей блокировкой, то есть атомарно относительно других потоков
//
// Параметры:
//   Индекс    - Число - Индекс, начиная с которого будут вставлены элементы
//   Коллекция - ЧитаемаяКоллекция - Коллекция, из которой будут вставлены элементы
//
//  Возвращаемое значение:
//   Булево - Истина, если был вставлен хотя бы один элемент
//
Функция ВставитьВсе(Индекс, Коллекция) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.ВставитьВсе(Индекс, Коллекция);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Заменяет все элементы в списке через алгоритм
//
// Блокировка удерживается на протяжении всей замены, поэтому длительный алгоритм
//  блокирует остальные потоки
//
// Параметры:
//   Алгоритм - Действие - Делегат на функцию с одним параметром, в которую передаётся
//    элемент списка, и которая возвращает новое значение элемента
//            - Строка - Лямбда выражение с одним параметром, в который передаётся
//    элемент списка, и которая возвращает новое значение элемента
//   Контекст - Структура - Контекст добавляемый в лямбда выражение, значения будут доступны по обращению
//    к ключу как к переменной.
//    При передачи действия в первом параметре, этот параметр игнорируется
//            - Сценарий - Экземпляр объекта, который будет захвачен в лямбда выражение, в лямбда выражении
//    будет доступен контекст (публичные и приватные поля, публичные методы) объекта.
//    При передачи действия в первом параметре, этот параметр игнорируется
//
// Пример:
//    Список.ЗаменитьВсе("Элемент -> Элемент + 1");
//
Процедура ЗаменитьВсе(Алгоритм, Контекст = Неопределено) Экспорт

	Блокировка.Заблокировать();

	Попытка
		ВнутреннийСписок.ЗаменитьВсе(Алгоритм, Контекст);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Разблокировать();

КонецПроцедуры

// Сортирует список в соответствии с переданным алгоритмом
//
// Сортировка выполняется под блокировкой, поэтому длительное сравнение
//  блокирует остальные потоки
//
// Параметры:
//   СравнениеЗначений - Действие - Делегат на функцию с двумя параметрами
//    в которую передаются 2 элемента списка, и которая возвращает число
//      1 - Если первый элемент больше,
//     -1 - Если второй элемент больше,
//      0 - Если элементы равны
//                     - Строка - Лямбда выражение с двумя параметрами
//    в которую передаются 2 элемента списка, и которая возвращает число
//      1 - Если первый элемент больше,
//     -1 - Если второй элемент больше,
//      0 - Если элементы равны
//   Контекст - Структура - Контекст добавляемый в лямбда выражение, значения будут доступны по обращению
//    к ключу как к переменной.
//    При передачи действия в первом параметре, этот параметр игнорируется
//            - Сценарий - Экземпляр объекта, который будет захвачен в лямбда выражение, в лямбда выражении
//    будет доступен контекст (публичные и приватные поля, публичные методы) объекта.
//    При передачи действия в первом параметре, этот параметр игнорируется
//
// Пример:
//    Список.Сортировать(СравнениеЗначений.ПрямойПорядок());
//
Процедура Сортировать(СравнениеЗначений, Контекст = Неопределено) Экспорт

	Блокировка.Заблокировать();

	Попытка
		ВнутреннийСписок.Сортировать(СравнениеЗначений, Контекст);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;

	Блокировка.Разблокировать();

КонецПроцедуры

// Удаляет элемент списка по индексу
//
// Вызывает исключение, если индекс выходит за границы списка
//
// Параметры:
//   Индекс - Число - Индекс удаляемого элемента
//
//  Возвращаемое значение:
//   Произвольный - Значение которое было удалено
//
Функция УдалитьПоИндексу(Индекс) Экспорт

	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.УдалитьПоИндексу(Индекс);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

// Устанавливает значение по индексу
//
// Вызывает исключение, если индекс выходит за границы списка
//
// Параметры:
//   Индекс   - Число - Индекс устанавливаемого элемента
//   Значение - Произвольный - Устанавливаемый элемент
//
//  Возвращаемое значение:
//   Произвольный - Значение которое было заменено
//
Функция Установить(Индекс, Значение) Экспорт
	
	Блокировка.Заблокировать();

	Попытка
		Результат = ВнутреннийСписок.Установить(Индекс, Значение);
	Исключение
		Блокировка.Разблокировать();
		ВызватьИсключение;
	КонецПопытки;
	
	Блокировка.Разблокировать();

	Возврат Результат;

КонецФункции

Процедура ОбработкаПолученияПредставления(Представление, СтандартнаяОбработка) // BSLLS:UnusedLocalMethod-off
	КоллекцииСлужебный.ОбработчикПолученияПредставленияКоллекции(ЭтотОбъект, Представление, СтандартнаяОбработка);
КонецПроцедуры

Функция ПолучитьИтератор() // BSLLS:UnusedLocalMethod-off
	Возврат Новый СлужебныйИтераторДляДвижка(Итератор());
КонецФункции

// Потокобезопасное представление списка
//
// Оборачивает переданный список и выполняет каждый его метод под блокировкой,
//  поэтому отдельный вызов метода атомарен относительно других потоков.
// Обёрнутый список не копируется, поэтому изменения, выполненные напрямую через него,
//  видны в представлении, но защиту блокировкой уже не получают - работать
//  со списком следует только через представление.
// Обход итератором не синхронизирован, последовательность операций
//  (например получение элемента по индексу и его замена) тоже не атомарна,
//  такие сценарии требуют внешней синхронизации по представлению
//
// Конструктор объекта
//
// Переданный список не копируется, представление работает поверх него
//
// Параметры:
//   Список - Список - Оборачиваемый список
//
// Пример:
//    Список = Новый СинхронизированныйСписок(Новый СписокМассив());
//
&Обходимое
&Реализует("Список")
Процедура ПриСозданииОбъекта(Список)
	
	ВнутреннийСписок  = Список;
	Блокировка        = Новый БлокировкаРесурса(ЭтотОбъект);

КонецПроцедуры
