#Использовать lambdas
#Использовать fluent
#Использовать optional

#Использовать "../../../internal"

Перем Массив;        // Элементы очереди
Перем ЕстьИтераторы; // Признак наличия созданных итераторов
Перем Компаратор;    // Компаратор, который приоритезирует очередь 

// Возвращает итератор очереди
//
// Обход выполняется в порядке приоритета элементов, то есть в порядке их извлечения.
// С момента создания первого итератора очередь начинает оповещать итераторы о своих изменениях,
//  поэтому изменение очереди в обход итератора делает ранее созданные итераторы невалидными
//
//  Возвращаемое значение:
//   Итератор - Итератор очереди
//
Функция Итератор() Экспорт

	ЕстьИтераторы = Истина;

	Возврат Новый ИтераторМассив(Массив, ЭтотОбъект);

КонецФункции

// Выполняет переданный алгоритм для каждого элемента очереди
//
// Обход выполняется через итератор очереди в порядке приоритета элементов,
//  элементы при этом из очереди не извлекаются, изменение очереди внутри алгоритма
//  приведёт к исключению
//
// Параметры:
//   Алгоритм - Действие - Делегат на процедуру с одним параметром, в который
//    будет передан элемент очереди
//            - Строка - Лямбда выражение с одним параметром, в который
//    будет передан элемент очереди
//   Контекст - Структура - Контекст добавляемый в лямбда выражение, значения будут доступны по обращению
//    к ключу как к переменной.
//    При передачи действия в первом параметре, этот параметр игнорируется
//            - Сценарий - Экземпляр объекта, который будет захвачен в лямбда выражение, в лямбда выражении
//    будет доступен контекст (публичные и приватные поля, публичные методы) объекта.
//    При передачи действия в первом параметре, этот параметр игнорируется
//
// Пример:
//    Очередь.ДляКаждого("Элемент -> Сообщить(Элемент)");
//
//    Очередь.ДляКаждого("Элемент -> Массив.Добавить(Элемент)", Новый Структура("Массив", Новый Массив));
//
Процедура ДляКаждого(Знач Алгоритм, Знач Контекст = Неопределено) Экспорт
	Итератор().ДляКаждогоОставшегося(Алгоритм, Контекст);
КонецПроцедуры

// Проверяет содержится ли указанный элемент в очереди
//
// Поиск элемента выполняется за линейное время
//
// Параметры:
//   Элемент - Произвольный - Проверяемый элемент
//
//  Возвращаемое значение:
//   Булево - Истина, если очередь содержит указанный элемент
//
Функция Содержит(Элемент) Экспорт
	Возврат Массив.Найти(Элемент) <> Неопределено;
КонецФункции

// Проверяет что очередь содержит все элементы переданной коллекции
//
// Проверка прекращается на первом ненайденном элементе
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой проверяются на вхождение
//
//  Возвращаемое значение:
//   Булево - Истина, если очередь содержит все элементы переданной коллекции
//
Функция СодержитВсе(Коллекция) Экспорт

	Результат = Истина;

	Итератор = Коллекция.Итератор();

	Пока Итератор.ЕстьСледующий() Цикл

		Если Не Содержит(Итератор.Следующий()) Тогда
			Результат = Ложь;
			Прервать;
		КонецЕсли;

	КонецЦикла;

	Возврат Результат;

КонецФункции

// Проверяет что очередь пустая
//
//  Возвращаемое значение:
//   Булево - Истина, если очередь пуста
//
Функция Пусто() Экспорт
	Возврат Количество() = 0;
КонецФункции

// Возвращает процессор коллекции по элементам очереди
//
// Процессор создаётся по копии элементов очереди, поэтому дальнейшие изменения очереди
//  в процессоре не отражаются
//
//  Возвращаемое значение:
//   ПроцессорКоллекций - Процессор коллекции
//
Функция ПроцессорКоллекции() Экспорт
	Возврат ПроцессорыКоллекций.ИзКоллекции(ВМассив());
КонецФункции

// Возвращает количество элементов очереди
//
//  Возвращаемое значение:
//   Число - Количество элементов очереди
//
Функция Количество() Экспорт
	Возврат Массив.Количество();
КонецФункции

// Выгружает элементы очереди в массив
//
// Возвращается копия элементов в порядке их приоритета, изменение полученного массива
//  на очередь не влияет
//
//  Возвращаемое значение:
//   Массив из Произвольный - Массив элементов очереди
//
Функция ВМассив() Экспорт
	Возврат Массивы.Скопировать(Массив);
КонецФункции

// Возвращает, но не удаляет элемент очереди с наибольшим приоритетом
//
//  Возвращаемое значение:
//   Опциональный - Первый элемент очереди, `Опциональные.Пустой()` если очередь пуста
//
Функция Подсмотреть() Экспорт

	Если Количество() > 0 Тогда
		Возврат Новый Опциональный(Массив[0]);
	КонецЕсли;

	Возврат Опциональные.Пустой();

КонецФункции

// Помещает элемент в очередь в соответствии с его приоритетом
//
// Метод эквивалентен методу `Положить` и реализован для совместимости с интерфейсом коллекции
//
// Параметры:
//   Элемент - Произвольный - Добавляемый элемент
//
//  Возвращаемое значение:
//   Булево - Всегда Истина
//
Функция Добавить(Элемент) Экспорт
	Возврат Положить(Элемент);
КонецФункции

// Помещает в очередь все элементы из переданной коллекции
//
// Порядок переданной коллекции не сохраняется, каждый элемент занимает место
//  в соответствии со своим приоритетом.
// Вызывает исключение, если коллекция содержит Неопределено, при этом ранее помещённые
//  элементы остаются в очереди
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой требуется добавить
//
//  Возвращаемое значение:
//   Булево - Истина, если был добавлен хотя бы один элемент
//
Функция ДобавитьВсе(Коллекция) Экспорт

	БылДобавленХотяБыОдинЭлемент = Ложь;

	Итератор = Коллекция.Итератор();

	Пока Итератор.ЕстьСледующий() Цикл

		БылДобавленХотяБыОдинЭлемент = Добавить(Итератор.Следующий())
			Или БылДобавленХотяБыОдинЭлемент;

	КонецЦикла;

	Возврат БылДобавленХотяБыОдинЭлемент;

КонецФункции

// Удаляет все элементы очереди
//
Процедура Очистить() Экспорт

	Массив.Очистить();
	ОповеститьОбИзмененииКоллекции();

КонецПроцедуры

// Удаляет первое вхождение переданного элемента из очереди
//
// Удаляется элемент, ближайший к началу очереди, дубликаты остаются в очереди
//
// Параметры:
//   Элемент - Произвольный - Удаляемый элемент
//
//  Возвращаемое значение:
//   Булево - Истина, если элемент был удалён
//
Функция Удалить(Элемент) Экспорт

	Индекс = Массив.Найти(Элемент);

	Если Индекс <> Неопределено Тогда

		Массив.Удалить(Индекс);
		ОповеститьОбИзмененииКоллекции();

		Возврат Истина;

	КонецЕсли;

	Возврат Ложь;

КонецФункции

// Удаляет из очереди элементы, которые содержатся в переданной коллекции
//
// Для каждого элемента переданной коллекции удаляется только первое его вхождение в очередь
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой будут удалены
//
//  Возвращаемое значение:
//   Булево - Истина, если был удалён хотя бы один элемент
//
Функция УдалитьВсе(Коллекция) Экспорт

	БылУдаленХотяБыОдинЭлемент = Ложь;

	Итератор = Коллекция.Итератор();

	Пока Итератор.ЕстьСледующий() Цикл

		БылУдаленХотяБыОдинЭлемент = Удалить(Итератор.Следующий())
			Или БылУдаленХотяБыОдинЭлемент;

	КонецЦикла;

	Возврат БылУдаленХотяБыОдинЭлемент;

КонецФункции

// Удаляет элементы очереди для которых функция предикат вернёт Истина
//
// Удаление выполняется за один проход через итератор очереди, поэтому удаляются
//  все вхождения подходящих элементов
//
// Параметры:
//   Предикат - Действие - Делегат на функцию с одним параметром, в которую передаётся элемент очереди
//            - Строка - Лямбда выражение с одним параметром, в который передаётся элемент очереди
//   Контекст - Структура - Контекст добавляемый в лямбда выражение, значения будут доступны по обращению
//    к ключу как к переменной.
//    При передачи действия в первом параметре, этот параметр игнорируется
//            - Сценарий - Экземпляр объекта, который будет захвачен в лямбда выражение, в лямбда выражении
//    будет доступен контекст (публичные и приватные поля, публичные методы) объекта.
//    При передачи действия в первом параметре, этот параметр игнорируется
//
//  Возвращаемое значение:
//   Булево - Истина, если был удалён хотя бы один элемент
//
// Пример:
//    Очередь.УдалитьЕсли("(Элемент) -> Элемент >= 2");
//
//    Очередь.УдалитьЕсли("(Элемент) -> Элемент >= Предел", Новый Структура("Предел", 2));
//
Функция УдалитьЕсли(Знач Предикат, Знач Контекст = Неопределено) Экспорт

	Если Пусто() Тогда
		Возврат Ложь;
	КонецЕсли;

	Если ТипЗнч(Предикат) = Тип("Строка") Тогда

		Предикат = Лямбда.Выражение(Предикат)
			.Интерфейс(ФункциональныеИнтерфейсы.УниФункция());

		Если ТипЗнч(Контекст) = Тип("Структура")
            Или ТипЗнч(Контекст) = Тип("ФиксированнаяСтруктура") Тогда
			Предикат.Контекст(Контекст);
		Иначе
			Предикат.ЗахватитьОбъект(Контекст);
		КонецЕсли;

		Предикат = Предикат.ВДействие();

	КонецЕсли;

	БылУдаленХотяБыОдинЭлемент = Ложь;

	Итератор = Итератор();

	Пока Итератор.ЕстьСледующий() Цикл

		Если Предикат.Выполнить(Итератор.Следующий()) Тогда

			Итератор.Удалить();

			БылУдаленХотяБыОдинЭлемент = Истина;

		КонецЕсли;

	КонецЦикла;

	Возврат БылУдаленХотяБыОдинЭлемент;

КонецФункции

// Удаляет из очереди все элементы которые не содержатся в переданной коллекции
//
// Для каждого элемента очереди выполняется проверка вхождения в переданную коллекцию,
//  поэтому сложность операции определяется реализацией переданной коллекции
//
// Параметры:
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементы которой будут сохранены
//
//  Возвращаемое значение:
//   Булево - Истина, если был удалён хотя бы один элемент
//
Функция СохранитьВсе(Коллекция) Экспорт

	БылУдаленХотяБыОдинЭлемент = Ложь;

	Итератор = Итератор();

	Пока Итератор.ЕстьСледующий() Цикл

		Если Не Коллекция.Содержит(Итератор.Следующий()) Тогда

			Итератор.Удалить();

			БылУдаленХотяБыОдинЭлемент = Истина;

		КонецЕсли;

	КонецЦикла;

	Возврат БылУдаленХотяБыОдинЭлемент;

КонецФункции

// Помещает элемент в очередь в соответствии с его приоритетом
//
// Место для элемента ищется перебором очереди от её начала, элемент встаёт после всех
//  элементов с таким же приоритетом.
// Очередь не ограничена по размеру, поэтому элемент помещается всегда.
// Вызывает исключение при попытке поместить Неопределено
//
// Параметры:
//  Элемент - Произвольный - Элемент, который требуется поместить в очередь
//
//  Возвращаемое значение:
//   Булево - Всегда Истина
//
Функция Положить(Элемент) Экспорт

	Если Элемент = Неопределено Тогда
		ВызватьИсключение "Очередь не может содержать Неопределено";
	КонецЕсли;

	Добавлен = Ложь;

	Для Индекс = 0 По Массив.ВГраница() Цикл

		Если Компаратор.Выполнить(Массив[Индекс], Элемент) > 0 Тогда
			Массив.Вставить(Индекс, Элемент);
			Добавлен = Истина;
			Прервать;
		КонецЕсли;

	КонецЦикла;

	Если Не Добавлен Тогда
		Массив.Добавить(Элемент);
	КонецЕсли;

	ОповеститьОбИзмененииКоллекции();

	Возврат Истина;

КонецФункции

// Возвращает и удаляет элемент очереди с наибольшим приоритетом
//
// Метод не ждёт появления элементов и сразу возвращает пустой опциональный для пустой очереди
//
//  Возвращаемое значение:
//   Опциональный - Первый элемент очереди, `Опциональные.Пустой()` если очередь пуста
//
Функция Взять() Экспорт

	Если Количество() > 0 Тогда

		Элемент = Массив[0];
		Массив.Удалить(0);
		ОповеститьОбИзмененииКоллекции();

		Возврат Новый Опциональный(Элемент);

	КонецЕсли;

	Возврат Опциональные.Пустой();

КонецФункции

Процедура ОповеститьОбИзмененииКоллекции()

	Если ЕстьИтераторы Тогда
		ВызватьСобытие("КоллекцияМодифицирована", Новый Массив);
	КонецЕсли;

КонецПроцедуры

Процедура ОбработкаПолученияПредставления(Представление, СтандартнаяОбработка) // BSLLS:UnusedLocalMethod-off
	КоллекцииСлужебный.ОбработчикПолученияПредставленияКоллекции(ЭтотОбъект, Представление, СтандартнаяОбработка);
КонецПроцедуры

Функция ПолучитьИтератор() // BSLLS:UnusedLocalMethod-off
	Возврат Новый СлужебныйИтераторДляДвижка(Итератор());
КонецФункции

Процедура УстановитьКомпаратор(пКомпаратор, Контекст)

	Если пКомпаратор = Неопределено Тогда

		Компаратор = СравнениеЗначений.ПрямойПорядок();

	ИначеЕсли ТипЗнч(пКомпаратор) = Тип("Строка") Тогда

		Компаратор = Лямбда.Выражение(пКомпаратор)
			.Интерфейс(ФункциональныеИнтерфейсы.БиФункция());

		Если ТипЗнч(Контекст) = Тип("Структура")
            Или ТипЗнч(Контекст) = Тип("ФиксированнаяСтруктура") Тогда
			Компаратор.Контекст(Контекст);
		Иначе
			Компаратор.ЗахватитьОбъект(Контекст);
		КонецЕсли;

		Компаратор = Компаратор.ВДействие();

	ИначеЕсли ТипЗнч(пКомпаратор) = Тип("Действие") Тогда

		Компаратор = пКомпаратор;

	Иначе
		ВызватьИсключение "Компаратором может выступать либо Действие либо лямбда-выражение";
	КонецЕсли;

КонецПроцедуры

// Приоритетная очередь, реализованная на базе массива
//
// В отличие от обычной очереди, порядок элементов определяется не временем помещения,
//  а компаратором - первым извлекается наименьший с точки зрения компаратора элемент.
// Позиция для нового элемента ищется перебором, поэтому помещение и извлечение элемента
//  выполняются за линейное время.
// Элементы с равным приоритетом извлекаются в порядке помещения.
// Очередь допускает дубликаты, но не может содержать Неопределено.
// Очередь не является потокобезопасной, для одновременной работы из нескольких потоков
//  следует использовать `СинхронизированнаяОчередь`
//
// Конструктор объекта
//
// Элементы переданной коллекции помещаются в очередь в соответствии с их приоритетом,
//  дальнейшие изменения переданной коллекции в очереди не отражаются
//
// Параметры:
//   Компаратор - Действие - Делегат на функцию с двумя параметрами
//    в которую передаются 2 элемента очереди, и которая возвращает число
//      1 - Если первый элемент больше,
//     -1 - Если второй элемент больше,
//      0 - Если элементы равны
//              - Строка - Лямбда выражение с двумя параметрами
//    в которое передаются 2 элемента очереди, и которое возвращает число
//      1 - Если первый элемент больше,
//     -1 - Если второй элемент больше,
//      0 - Если элементы равны
//    Если компаратор не передан, элементы упорядочиваются по возрастанию значений
//   Коллекция - ЧитаемаяКоллекция - Коллекция, элементами которой будет заполнена очередь
//   Контекст - Структура - Контекст добавляемый в лямбда выражение, значения будут доступны по обращению
//    к ключу как к переменной.
//    При передачи действия в первом параметре, этот параметр игнорируется
//            - Сценарий - Экземпляр объекта, который будет захвачен в лямбда выражение, в лямбда выражении
//    будет доступен контекст (публичные и приватные поля, публичные методы) объекта.
//    При передачи действия в первом параметре, этот параметр игнорируется
//
// Пример:
//    Очередь = Новый ПриоритетнаяОчередь();
//
//    Очередь = Новый ПриоритетнаяОчередь(СравнениеЗначений.ОбратныйПорядок());
//
//    Очередь = Новый ПриоритетнаяОчередь("(Первый, Второй) -> Первый.Вес - Второй.Вес", Задачи);
//
&Обходимое
&Реализует("Очередь")
Процедура ПриСозданииОбъекта(Компаратор = Неопределено, Коллекция = Неопределено, Контекст = Неопределено)

	Массив        = Новый Массив;
	ЕстьИтераторы = Ложь;

	УстановитьКомпаратор(Компаратор, Контекст);

	Если Коллекция <> Неопределено Тогда
		ДобавитьВсе(Коллекция);
	КонецЕсли;

КонецПроцедуры
